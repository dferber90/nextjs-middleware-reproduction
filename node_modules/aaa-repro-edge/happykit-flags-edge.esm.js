import { getConfig } from 'aaa-repro-config';

function getEdgeFlags() {
  // getConfig() should return a value but returns null here
  // this happens because aaa-repro-config gets inlined into the build twice
  return getConfig();
}

export { getEdgeFlags, getConfig };

