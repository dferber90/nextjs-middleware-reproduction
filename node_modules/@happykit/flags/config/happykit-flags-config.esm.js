var config = null; // getter is necessary as we can't export a live binding of "config"

const rnd = Math.random()

function getConfig() {
  console.log("get config", rnd)
  return config;
}

function configure() {
  console.log("set config", rnd)

  config = true;
}

export { configure, getConfig };
