import { getConfig } from '../config/happykit-flags-config.esm.js';

function getEdgeFlags(options) {
  var config = getConfig();

  return {configured: config ? true : false}
}

export { getEdgeFlags, getConfig };

